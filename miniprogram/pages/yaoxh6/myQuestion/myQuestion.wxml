<view class="info" wx:for="{{questionInfo}}" wx:key="id">
  <view class='infoItem'>
      <view class="infoName">{{item.question_title}}</view>
      <button class="weui-btn inLineButton" data-index="{{index}}" bindtap='showQADetail'>问题详情</button>
  </view>
</view>
